package com.jackpot.whiskeynote.domain.community.entity;

public enum PostCategory {
    F,  // Free (자유)
    R,  // Review (리뷰)
    L,  // Location (장소)
    Q,  // Question (질문)
    G,  // Give (나눔)
    B   // Club (동호회)
}